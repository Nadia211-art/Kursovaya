<Window x:Class="Kursovaya.View.Windows.EditSectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursovaya.View.Windows"
        mc:Ignorable="d"
                Title="Изменить секцию" SizeToContent="WidthAndHeight" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Background="#FFD4DFE7">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="10" />
            <RowDefinition Height="40" />
            <RowDefinition Height="10" />
            <RowDefinition Height="40" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>

        <TextBlock Text="Название:" VerticalAlignment="Center"/>
        <TextBox Name="NameTb" Grid.Column="2" MaxLength="150" Text="{Binding Title}"/>

        <TextBlock Text="Фото:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox Name="PhotoTb" Grid.Column="2" Grid.Row="2" Text="{Binding Photo}"/>
        <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="4" HorizontalAlignment="Center">
            <Button Content="Редактировать" Name="EditBtn" Click="EditBtn_Click" Width="175"/>
            <Button Content="Загрузить" Name="LoadFromPCBtn" Click="LoadFromPCBtn_Click" Margin="5 0"/>
        </StackPanel>
        

    </Grid>
</Window>
